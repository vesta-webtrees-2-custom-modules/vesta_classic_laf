<?php

use Cissee\WebtreesExt\Services\GedcomEditServiceExt2;
use Fisharebest\Webtrees\Services\GedcomEditService;
use Fisharebest\Webtrees\Tree;

/**
 * @var GedcomEditService $gedcom_edit_service
 * @var string            $post_url
 * @var string            $title
 * @var Tree              $tree
 * @var string            $url
 */

//[RC adjusted]
$gedcom_edit_service = new GedcomEditServiceExt2(true);

//explicitly register in order to re-use in views where we cannot pass via variable
app()->instance(GedcomEditServiceExt2::class, $gedcom_edit_service);

//call original view
echo view('::edit/new-individual', [
    'facts'               => $facts,
    'gedcom_edit_service' => $gedcom_edit_service,
    'post_url'            => $post_url,
    'title'               => $title,
    'tree'                => $tree,
    'url'                 => $url,
]);

